{{#extend "mail/layouts/main"}}
  {{#content "content"}}
    <p>尊敬的 {{ fullName }}, {{ expense.employee.firstName }} {{ expense.employee.lastName }} 申請報銷, 按照下面的鏈接查看驗收的完整細節</p>
    <p><a href="{{siteUrl}}/employees/expense/{{expense.id}}">{{siteUrl}}/employees/expense/{{expense.id}}</a></p>
    <p>如果仍有問題, 請聯繫YOOV支持.</p>
    <p>{{ siteSupportEmail }}</p>
    <p>這是一個自動生成的電子郵件, 請不要回覆 ***</p>
  {{/content}}
{{/extend}}
